export enum PUBLIC_ROUTE {
  UNKNOWN = '#',
  HOME = '/',
  LOGIN = '/auth/login',
  SIGNUP = '/auth/signup',
  NOT_FOUND = '*',
}

export enum PRIVATE_ROUTE {
  DASHBOARD = '/dashboard',
  USER_PROFILE = '/profile',
}
